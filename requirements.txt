# OP-RRD core deps (dev + CPU unit tests). For the GPU training run also
# install requirements-gpu.txt (bitsandbytes / vllm / lm-eval).
numpy<2
torch>=2.2,<2.6
transformers>=4.44,<4.50
peft>=0.11
accelerate>=0.30
datasets>=2.20,<3.0
pyarrow==16.1.0
pyyaml
pytest
